# Store the above values in a list & sort them from "A" to "D". ["A", "B", "A", "C", "A", "D"]

# Store the above values in a list
grades = ["A", "B", "A", "C", "A", "D"] 
# Sort the list from "A" to "D"
grades.sort()
print(grades) # Output: ['A', 'A', 'A', 'B', 'C', 'D']

